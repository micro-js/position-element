{
  "name": "@f/position-element",
  "description": "Position an element relative to another element",
  "repository": "git://github.com/micro-js/position-element.git",
  "version": "1.1.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/apply-styles": "^1.0.1",
    "@f/compute-placement": "^1.1.0",
    "@f/element-rect": "^1.0.0",
    "@f/offset-parent": "^1.0.0"
  },
  "devDependencies": {
    "hihat": "^2.6.4",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.4.0"
  }
}
